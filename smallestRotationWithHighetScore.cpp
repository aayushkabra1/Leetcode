#include <iostream>
#include <vector>
using namespace std;

class Solution {
public:
    int bestRotation(vector<int>& nums) {
        int ans = 0;
        for (int rot = 0; rot < nums.size(); rot++) {
            int tempAns = 0;
            for (int i = 0; i < nums.size(); i++) {
                
            }
        }
    }
};